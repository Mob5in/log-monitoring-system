# Active only when running with --spring.profiles.active=evaluator
# An evaluator-only instance consumes from Kafka and writes to Postgres,
# but doesn't serve HTTP -> no need for an embedded web server.

spring.main.web-application-type=none