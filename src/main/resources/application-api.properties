# Active only when running with --spring.profiles.active=api
# An API-only instance needs the database (to read alerts) and the embedded
# web server, but doesn't need to produce/consume anything from Kafka.
# Nothing to override here yet ? Kafka autoconfiguration is cheap when no
# @KafkaListener bean exists in this profile's context (LogConsumerService
# isn't active here), so there's no real connection overhead to strip out.